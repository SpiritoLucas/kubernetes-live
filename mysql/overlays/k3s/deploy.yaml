apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
spec:
  selector:
    matchLabels:
      app: mysql
  replicas: 1
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
        - name: mysql
          image: mysql:latest
          env:
            - name: MYSQL_DATABASE
              value: 'Broidery-api-db'
            - name: MYSQL_USER
              value: 'broidery-user'
            - name: MYSQL_PASSWORD
              value: 'gQ93Q1ihANsg+Nmp'
            - name: MYSQL_ROOT_PASSWORD
              value: "EGwyETUz+eUOGuDe"
          ports:
            - containerPort: 3306
              name: mysql
          volumeMounts:
            - name: mysql-persistent-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-persistent-storage
          hostPath:
            path: /mnt/data/mysql
            type: DirectoryOrCreate

